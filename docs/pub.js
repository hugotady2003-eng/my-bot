/* ══════════════════════════════════════════════════════════════════════
   PUBLICITÉ — LE SEUL FICHIER À MODIFIER

   Colle ton identifiant AdSense entre les guillemets ci-dessous, puis
   redépose ce fichier. C'est tout : les huit pages le lisent.

   ⚠️ Tant qu'il est vide, AUCUNE requête n'est envoyée à Google. Le site
      reste rapide et fonctionne normalement, simplement sans annonces.
      Tu peux donc le mettre en ligne dès maintenant et revenir ici le
      jour où ton compte est approuvé.

   Format attendu :  ca-pub-XXXXXXXXXXXXXXXX
   Où le trouver : adsense.google.com → Compte → Informations sur le compte
   ══════════════════════════════════════════════════════════════════════ */

window.PUB_ID = "";


/* ── Emplacements (facultatif) ─────────────────────────────────────────
   AdSense fonctionne très bien sans identifiant de bloc : il choisit le
   format tout seul. Ne remplis ceci que si tu as créé des blocs nommés
   dans ton compte et que tu veux suivre leurs performances séparément. */

window.PUB_SLOTS = {
  cote:  "",   /* colonnes latérales, PC */
  bloc:  "",   /* carré au milieu de l'article, téléphone */
};
